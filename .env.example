# Copy to .env.local (gitignored) and fill in your own value.
# The map works with no key at all — CARTO's basemap tiles are free and
# anonymous. This only raises the rate limit; see src/components/ShipMap.tsx.
VITE_CARTO_KEY=
